import React from "react";
import ReactDOM from "react-dom/client";
import { Nav2Panel } from "./components/Nav2/Nav2Panel";
import "./global.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <Nav2Panel />
  </React.StrictMode>,
);
